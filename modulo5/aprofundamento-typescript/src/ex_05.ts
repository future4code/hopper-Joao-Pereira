// Exercício 5

// Existem algumas consigurações no exemplo que nao estão nas configurações que fizemos pelo slide. São elas: 

// "strict": true, /* Enable all strict type-checking options. */ Habilita todas as opções de verificação de tipo estrita. 

// "esModuleInterop": true, /* Enables emit interoperability between CommonJS and ES Modules via creation of namespace objects for all imports. Implies 'allowSyntheticDefaultImports'. */Habilita a interoperabilidade de emissão entre os módulos CommonJS e ES através da criação de objetos de namespace para todas as importações

// "forceConsistentCasingInFileNames": true  /* Disallow inconsistently-cased references to the same file. */ Não permite referências com maiúsculas e minúsculas inconsistentes para o mesmo arquivo.


// -------------------------CONFIGURAÇOES DO SLIDE

        // {
        //"compilerOptions": {
        //     "target": "es6", /* Specify ECMAScript target version */
        //     "module": "commonjs", /* Specify module code generation */
        //     "sourceMap": true, /* Generates corresponding '.map' file. */
        //     "outDir": "./build", /* Redirect output structure to the directory. */
        //     "rootDir": "./src", /* Specify the root directory of input files. */
        //     "removeComments": true, /* Do not emit comments to output. */
        //     "noImplicitAny": true, /* Raise error on declarations with an implied 'any' type. */
        //     }
        // }
        